import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.jimmyshian.runic',
  appName: 'runic-synthesis',
  webDir: 'dist'
};

export default config;
